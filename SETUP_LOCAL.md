# 🦁 BannerGym 本物のAI採点を動かす手順（ローカル・テスト用）

自分のパソコンの中だけで、コーチ（AI）が実際に画像を見て採点する版を動かします。
むずかしくないで！順番にいこか。

---

## 用意するもの（3つのファイルを同じフォルダに）

ひとつのフォルダ（例：デスクトップに `bannergym` フォルダを作る）に、次の3つを入れてください👇

- `bannergym.html` … アプリ本体（送ったやつ）
- `server.js` … 中継サーバー（送ったやつ）
- `apikey.txt` … あなたのAPIキーを書くファイル（これから作る）

---

## STEP 1：Node.js を入れる（初回だけ）

中継サーバーを動かすのに「Node.js」が必要です。

1. すでに入ってるか確認：Macの「ターミナル」を開いて、次を打ってEnter
   ```
   node -v
   ```
   `v18` 以上の数字が出ればOK（STEP 2へ）。
2. 出てこない／古い場合：[https://nodejs.org](https://nodejs.org) を開いて、**LTS版**をダウンロードしてインストール。

> ターミナルの開き方：`command + スペース` → 「ターミナル」と入力 → Enter

---

## STEP 2：APIキーをファイルに入れる

1. `bannergym` フォルダの中に、**`apikey.txt`** という名前のテキストファイルを作る
2. その中に、取得した **`sk-ant-...` のキーだけ**を貼り付けて保存
   （余計な文字やスペース、改行はなしで、キーだけ）

> 🔒 このファイルは**自分のPCの中だけ**。人に渡したり、ネットに上げたりせんといてな。

---

## STEP 3：サーバーを起動する

1. ターミナルを開く
2. フォルダに移動する。`cd ` と打って（cdのあとに半角スペース）、`bannergym` フォルダをターミナルにドラッグ&ドロップ → Enter
   （例：`cd /Users/あなた/Desktop/bannergym`）
3. 次を打ってEnter：
   ```
   node server.js
   ```
4. こう出たら成功🎉
   ```
   🦁 BannerGym 起動したで！ ブラウザで  http://localhost:3000  を開いてな
   ```

---

## STEP 4：使う

1. ブラウザで **`http://localhost:3000`** を開く
   （※ HTMLファイルを直接ダブルクリックではなく、必ずこのURLから開く！）
2. お題を受け取って、作品画像をアップ → 提出
3. コーチが**実際に画像を見て**、関西弁で採点・添削してくれます🦁🔥

終わるときは、ターミナルで `control + C` を押せばサーバーが止まります。

---

## うまくいかない時

- **「apikey.txt が見当たらへん」と出る** → ファイル名が合ってるか確認（`apikey.txt`）。`bannergym` フォルダの中にあるか確認。
- **採点するとデモのままになる** → `http://localhost:3000` から開いているか確認（ファイル直開きだとデモになる）。サーバーが起動中か確認。
- **エラーが出る** → ターミナルに出る赤い文字（エラー内容）をスクショして送ってくれたら、一緒に直すで。
- **お金が心配** → Anthropicの「Billing」で使った金額が見られます。上限（Spend limit）を設定しておくと安心。

---

困ったら、いつでも聞いてな！🦁💪
